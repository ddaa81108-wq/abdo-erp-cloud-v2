rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 🔒 FIX #3: Locked down — only authenticated users can read/write
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
